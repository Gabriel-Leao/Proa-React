import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const rita = ReactDOM.createRoot(document.getElementById('root'));
rita.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);